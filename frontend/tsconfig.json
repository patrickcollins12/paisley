{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}

// TODO FIX:
// No inputs were found in config file 'paisley_frontend/tsconfig.json'. 
// Specified 'include' paths were '["**/*"]' and 'exclude' paths were '[]'.ts